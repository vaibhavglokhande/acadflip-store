import { EvenOddDirective } from './even-odd.directive';

describe('EvenOddDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenOddDirective();
    expect(directive).toBeTruthy();
  });
});
